# Automation Artifacts

This directory is reserved for automation outputs.

Policy:
- Generated run artifacts (e.g. `latest.json`, dated reports, transient outputs) are ignored by `.gitignore`.
- Only intentional documentation files (such as this README) should be tracked.
